<Window x:Class="HCIDE.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
        Title="HCIDE" 
        Height="900" Width="1600"
        WindowState="Maximized"
        Background="#1E1E1E">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        
                <Style x:Key="ToolbarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3E3E42"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2D2D30"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#555555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Key="N" Modifiers="Control" Command="{Binding NewFileCommand}"/>
        <KeyBinding Key="S" Modifiers="Control" Command="{Binding SaveFileCommand}"/>
        <KeyBinding Key="S" Modifiers="Control+Shift" Command="{Binding SaveAllCommand}"/>
        <KeyBinding Key="F" Modifiers="Control" Command="{Binding FindCommand}"/>
        <KeyBinding Key="F5" Command="{Binding RunProjectCommand}"/>
    </Window.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Menu Bar -->
        <Menu Grid.Row="0" Background="#2D2D30" Foreground="White">
            <Menu.ItemsPanel>
                <ItemsPanelTemplate>
                    <DockPanel/>
                </ItemsPanelTemplate>
            </Menu.ItemsPanel>

            <MenuItem Header="_File">
                <MenuItem Header="_New File" Command="{Binding NewFileCommand}" InputGestureText="Ctrl+N"/>
                <MenuItem Header="_Open File..." Command="{Binding OpenFileCommand}"/>
                <Separator/>
                <MenuItem Header="_Save" Command="{Binding SaveFileCommand}" InputGestureText="Ctrl+S"/>
                <MenuItem Header="Save _All" Command="{Binding SaveAllCommand}" InputGestureText="Ctrl+Shift+S"/>
                <Separator/>
                <MenuItem Header="E_xit" Command="{Binding ExitCommand}"/>
            </MenuItem>

            <MenuItem Header="_Edit">
                <MenuItem Header="Find..." Command="{Binding FindCommand}" InputGestureText="Ctrl+F"/>
            </MenuItem>

            <MenuItem Header="_Run">
                <MenuItem Header="_Run Project" Command="{Binding RunProjectCommand}" InputGestureText="F5"/>
                <MenuItem Header="_Stop" Command="{Binding StopCommand}"/>
            </MenuItem>

            <MenuItem Header="_Settings">
                <MenuItem Header="_Theme Settings..." Command="{Binding OpenThemeSettingsCommand}"/>
            </MenuItem>
        </Menu>

        <!-- Tool Bar -->
        <ToolBar Grid.Row="1" Background="#333337" BorderBrush="#3E3E42" BorderThickness="0,0,0,1">
            <Button Command="{Binding NewFileCommand}" 
                    ToolTip="New File (Ctrl+N)"
                    Content="📄"
                    FontSize="18"
                    Padding="10,5"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"/>
            <Button Command="{Binding SaveFileCommand}" 
                    ToolTip="Save (Ctrl+S)"
                    Content="💾"
                    FontSize="18"
                    Padding="10,5"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"/>
            <Separator/>
            <Button Command="{Binding RunProjectCommand}" 
                    ToolTip="Run (F5)"
                    Content="▶️"
                    FontSize="18"
                    Padding="10,5"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
            <Button Command="{Binding StopCommand}" 
                    ToolTip="Stop"
                    Content="⏹️"
                    FontSize="18"
                    Padding="10,5"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    IsEnabled="{Binding IsRunning}"/>
        </ToolBar>

        <!-- Main Content Area -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" MinWidth="150"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- File Explorer (Left Panel) -->
            <Border Grid.Column="0" 
                    Background="#252526" 
                    BorderBrush="#3E3E42" 
                    BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Explorer Header -->
                    <Border Grid.Row="0" 
                            Background="#1E1E1E"
                            BorderBrush="#3E3E42"
                            BorderThickness="0,0,0,1"
                            Padding="15,10">
                        <TextBlock Text="EXPLORER" 
                                   Foreground="#CCCCCC"
                                   FontSize="11"
                                   FontWeight="Bold"/>
                    </Border>

                    <!-- File Tree -->
                    <TreeView Grid.Row="1" 
                              ItemsSource="{Binding FileTree}"
                              Background="Transparent"
                              Foreground="White"
                              BorderThickness="0"
                              Padding="5">
                        <TreeView.ItemContainerStyle>
                            <Style TargetType="TreeViewItem">
                                <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Padding" Value="3"/>
                            </Style>
                        </TreeView.ItemContainerStyle>
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                <StackPanel Orientation="Horizontal" 
                                           MouseLeftButtonDown="FileTreeItem_MouseLeftButtonDown">
                                    <TextBlock Text="{Binding Icon}" 
                                              Margin="0,0,5,0"
                                              FontSize="14"/>
                                    <TextBlock Text="{Binding Name}"
                                              FontSize="13"/>
                                </StackPanel>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </Grid>
            </Border>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1" 
                          Width="5" 
                          HorizontalAlignment="Stretch" 
                          Background="#3E3E42"
                          ShowsPreview="False"/>

            <!-- Editor Area (Center) -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="5"/>
                    <RowDefinition Height="200" MinHeight="100"/>
                </Grid.RowDefinitions>

                <!-- Tab Control for Open Files -->
                <TabControl Grid.Row="0" 
                            ItemsSource="{Binding OpenFiles}"
                            SelectedItem="{Binding SelectedFile}"
                            Background="#1E1E1E"
                            BorderThickness="0">
                    <TabControl.ItemContainerStyle>
                        <Style TargetType="TabItem">
                            <Setter Property="Background" Value="#2D2D30"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="12,8"/>
                        </Style>
                    </TabControl.ItemContainerStyle>

                    <TabControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}" 
                                           Foreground="White"
                                           VerticalAlignment="Center"/>
                                <TextBlock Text="*" 
                                           Foreground="#E74856"
                                           Margin="3,0,5,0"
                                           VerticalAlignment="Center"
                                           Visibility="{Binding IsDirty, Converter={StaticResource BoolToVis}}"/>
                                <Button Content="×" 
                                        Command="{Binding DataContext.CloseFileCommand, RelativeSource={RelativeSource AncestorType=TabControl}}"
                                        CommandParameter="{Binding}"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Foreground="White"
                                        FontSize="18"
                                        Padding="5,0"
                                        Margin="5,0,0,0"
                                        VerticalAlignment="Center"
                                        Cursor="Hand"/>
                            </StackPanel>
                        </DataTemplate>
                    </TabControl.ItemTemplate>

                    <TabControl.ContentTemplate>
                        <DataTemplate>
                            <!-- AvalonEdit Code Editor -->
                            <avalonedit:TextEditor x:Name="CodeEditor"
                                                   Loaded="CodeEditor_Loaded"
                                                   Background="{DynamicResource EditorBackground}"
                                                   Foreground="{DynamicResource EditorForeground}"
                                                   FontFamily="{Binding DataContext.ThemeSettings.FontFamily, RelativeSource={RelativeSource AncestorType=Window}}"
                                                   FontSize="{Binding DataContext.ThemeSettings.FontSize, RelativeSource={RelativeSource AncestorType=Window}}"
                                                   ShowLineNumbers="True"
                                                   LineNumbersForeground="{DynamicResource LineNumberForeground}"
                                                   SyntaxHighlighting="{Binding SyntaxHighlighting}"
                                                   Document="{Binding Document}"
                                                   VerticalScrollBarVisibility="Auto"
                                                   HorizontalScrollBarVisibility="Auto">
                                <avalonedit:TextEditor.Options>
                                    <avalonedit:TextEditorOptions 
                                        ShowSpaces="False" 
                                        ShowTabs="False"
                                        ConvertTabsToSpaces="True"
                                        IndentationSize="4"
                                        HighlightCurrentLine="True"/>
                                </avalonedit:TextEditor.Options>
                            </avalonedit:TextEditor>
                        </DataTemplate>
                    </TabControl.ContentTemplate>
                </TabControl>

                <!-- Splitter -->
                <GridSplitter Grid.Row="1" 
                              Height="5" 
                              HorizontalAlignment="Stretch" 
                              Background="#3E3E42"
                              ShowsPreview="False"/>

                <!-- Output Panel -->
                <Border Grid.Row="2" 
                        Background="#1E1E1E" 
                        BorderBrush="#3E3E42" 
                        BorderThickness="0,1,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Output Header -->
                        <Border Grid.Row="0"
                                Background="#2D2D30"
                                BorderBrush="#3E3E42"
                                BorderThickness="0,0,0,1"
                                Padding="15,8">
                            <TextBlock Text="OUTPUT" 
                                       Foreground="#CCCCCC"
                                       FontSize="11"
                                       FontWeight="Bold"/>
                        </Border>

                        <!-- Output Text -->
                        <ScrollViewer Grid.Row="1" 
                                     VerticalScrollBarVisibility="Auto"
                                     HorizontalScrollBarVisibility="Auto">
                            <TextBox Text="{Binding OutputText, Mode=OneWay}"
                                     Background="#1E1E1E"
                                     Foreground="#CCCCCC"
                                     FontFamily="Consolas"
                                     FontSize="12"
                                     IsReadOnly="True"
                                     TextWrapping="NoWrap"
                                     BorderThickness="0"
                                     Padding="15"
                                     AcceptsReturn="True"
                                     VerticalScrollBarVisibility="Disabled"
                                     HorizontalScrollBarVisibility="Disabled"/>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3" 
                   Background="#007ACC" 
                   Height="25"
                   BorderThickness="0">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusBarText}" 
                           Foreground="White"
                           FontSize="12"
                           Margin="10,0"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal" Margin="10,0">
                    <TextBlock Text="Ln " Foreground="White" FontSize="12"/>
                    <TextBlock Text="{Binding CurrentLine}" Foreground="White" FontSize="12" Margin="0,0,10,0"/>
                    <TextBlock Text="Col " Foreground="White" FontSize="12"/>
                    <TextBlock Text="{Binding CurrentColumn}" Foreground="White" FontSize="12" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding CurrentLanguage}" Foreground="White" FontSize="12" FontWeight="Bold"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>