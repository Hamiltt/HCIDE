<Window x:Class="HCIDE.Views.ProjectSelectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="HCIDE - Project Selector" 
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#2D2D30"
        xmlns:converters="clr-namespace:HCIDE.Utils.Converters"
        ResizeMode="CanResize">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <converters:StringNotEmptyToVisibilityConverter x:Key="StringNotEmptyToVisibilityConverter"/>
        <converters:InverseBooleanConverter x:Key="InverseBoolConverter"/>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#0E639C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1177BB"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0D5A8F"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#3E3E42"/>
                                <Setter Property="Foreground" Value="#858585"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- FIXED: TabItem Style - теперь читаемые вкладки! -->
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="#3E3E42"
                                BorderThickness="0,0,1,0"
                                CornerRadius="0"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Selected state - активная вкладка -->
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#1E1E1E"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <!-- Hover state - при наведении -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3E3E42"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <!-- Default state - неактивная вкладка -->
                            <Trigger Property="IsSelected" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#2D2D30"/>
                                <Setter Property="Foreground" Value="#CCCCCC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="HCIDE" 
                       FontSize="42" 
                       FontWeight="Bold"
                       Foreground="#569CD6"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Multi-Language Development Environment" 
                       FontSize="14" 
                       Foreground="#CCCCCC"
                       HorizontalAlignment="Center"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Tab Control -->
        <TabControl Grid.Row="1" 
                    Background="#252526"
                    BorderBrush="#3E3E42"
                    BorderThickness="1">

            <!-- New Project Tab -->
            <TabItem Header="📁 New Project">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="30">
                        <TextBlock Text="Project Name:" 
                                   Foreground="White" 
                                   FontSize="14"
                                   Margin="0,10,0,8"/>
                        <TextBox Text="{Binding ProjectName, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="12"
                                 FontSize="14"
                                 Background="#3E3E42"
                                 Foreground="White"
                                 BorderBrush="#569CD6"
                                 BorderThickness="1"/>

                        <TextBlock Text="Project Location:" 
                                   Foreground="White" 
                                   FontSize="14"
                                   Margin="0,20,0,8"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0"
                                     Text="{Binding ProjectPath}"
                                     Padding="12"
                                     FontSize="14"
                                     Background="#3E3E42"
                                     Foreground="White"
                                     BorderBrush="#569CD6"
                                     BorderThickness="1"/>
                            <Button Grid.Column="1" 
                                    Content="Browse..." 
                                    Command="{Binding BrowsePathCommand}"
                                    Margin="10,0,0,0"
                                    Padding="20,12"
                                    Style="{StaticResource PrimaryButton}"/>
                        </Grid>

                        <TextBlock Text="Project Type:" 
                                   Foreground="White" 
                                   FontSize="14"
                                   Margin="0,20,0,8"/>
                        <ComboBox SelectedIndex="{Binding SelectedProjectTypeIndex}"
                                  Padding="12"
                                  FontSize="14"
                                  Background="#3E3E42"
                                  Foreground="White"
                                  BorderBrush="#569CD6"
                                  BorderThickness="1">
                            <ComboBoxItem Content="🐍 Python 3.12"/>
                            <ComboBoxItem Content="📜 JavaScript (Node.js)"/>
                            <ComboBoxItem Content="🔷 Go 1.21"/>
                        </ComboBox>

                        <Button Content="Create Project" 
                                Command="{Binding CreateProjectCommand}"
                                IsEnabled="{Binding IsCreatingProject, Converter={StaticResource InverseBoolConverter}}"
                                Margin="0,30,0,0"
                                Padding="30,15"
                                FontSize="16"
                                FontWeight="Bold"
                                HorizontalAlignment="Center"
                                Style="{StaticResource PrimaryButton}"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Recent Projects Tab -->
            <TabItem Header="🕒 Recent Projects">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <ListBox Grid.Row="0"
                             ItemsSource="{Binding RecentProjects}"
                             SelectedItem="{Binding SelectedRecentProject}"
                             Background="#252526"
                             Foreground="White"
                             BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="15" 
                                        Margin="5"
                                        Background="#2D2D30"
                                        BorderBrush="#3E3E42"
                                        BorderThickness="1"
                                        CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" 
                                                   FontSize="18" 
                                                   FontWeight="Bold"
                                                   Foreground="White"/>
                                        <TextBlock Text="{Binding Path}" 
                                                   FontSize="12" 
                                                   Foreground="#CCCCCC"
                                                   Margin="0,5,0,0"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                            <TextBlock FontSize="11" Foreground="#858585">
                                                <Run Text="{Binding Type}"/>
                                                <Run Text=" • "/>
                                                <Run Text="Last opened: "/>
                                                <Run Text="{Binding LastOpened, StringFormat='yyyy-MM-dd HH:mm'}"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <Button Grid.Row="1"
                            Content="Open Selected Project" 
                            Command="{Binding OpenRecentProjectCommand}"
                            Margin="0,15,0,0"
                            Padding="25,12"
                            FontSize="14"
                            HorizontalAlignment="Center"
                            Style="{StaticResource PrimaryButton}"/>
                </Grid>
            </TabItem>

            <!-- Open Existing Tab -->
            <TabItem Header="📂 Open Existing">
                <StackPanel Margin="30" 
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center">
                    <TextBlock Text="📂" 
                               FontSize="80"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,20"/>
                    <TextBlock Text="Open an existing project folder" 
                               FontSize="16"
                               Foreground="#CCCCCC"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,30"/>
                    <Button Content="Browse for Project Folder..." 
                            Command="{Binding OpenExistingCommand}"
                            Padding="30,15"
                            FontSize="16"
                            HorizontalAlignment="Center"
                            Style="{StaticResource PrimaryButton}"/>
                </StackPanel>
            </TabItem>
        </TabControl>

        <!-- Progress Section -->
        <Border Grid.Row="2" 
                Margin="0,20,0,0"
                Padding="15"
                Background="#1E1E1E"
                BorderBrush="#3E3E42"
                BorderThickness="1"
                CornerRadius="4"
                Visibility="{Binding IsDownloading, Converter={StaticResource BoolToVis}}">
            <StackPanel>
                <TextBlock Text="{Binding StatusMessage}"
                           Foreground="White"
                           FontSize="14"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10"/>
                <ProgressBar Height="8"
                             Value="{Binding DownloadProgress}"
                             Maximum="100"
                             Foreground="#0E639C"
                             Background="#3E3E42"
                             BorderThickness="0"/>
                <TextBlock Text="{Binding DownloadProgress, StringFormat='{}{0:F1}%'}"
                           Foreground="#CCCCCC"
                           FontSize="12"
                           HorizontalAlignment="Center"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="3" 
                Margin="0,15,0,0"
                Padding="10"
                Background="#007ACC"
                Visibility="{Binding StatusMessage, Converter={StaticResource StringNotEmptyToVisibilityConverter}}">
            <TextBlock Text="{Binding StatusMessage}"
                       Foreground="White"
                       FontSize="12"
                       HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>